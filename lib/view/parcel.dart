import 'package:flutter/material.dart';

class Parcel extends StatelessWidget {
  const Parcel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is Parcel Page"),
      ),
    );
  }
}
